# iOS → Android parity backlog

Источник: чейндж-лог iOS-разработчика (2026-08-11). Цель — довести Android до
паритета, реализуя всё возможное **на фронте нашим дизайном** (бэкенд не
меняем). Выполняем последовательно, каждый пункт — с дабл-чеком на устройствах.

Статусы: ⬜ TODO · 🔧 IN PROGRESS · ✅ DONE · ⏸ BLOCKED(backend) · ➖ уже есть

## Приоритет 0 — Звонки (переподключения + завершение — главный фокус)

| # | Задача | Статус | Заметки |
|---|---|---|---|
| C1 | Выбор завершения звонка: «Выйти» vs «Завершить для всех» | ✅ | Сделано: `POST /calls/dialog/{dialogId}/stop` + `endCallForEveryone()`; красная кнопка: 1-на-1 = завершить у обоих (REST stop), админ группы = лист выбора Выйти/Завершить для всех, участник группы = Выйти. Нужен дабл-чек на устройствах. |
| C2 | Сетка участников при 5+ | ✅ | При >4 участниках видео/аудио-сетка вертикально скроллится, строки фикс. высоты 200dp, плитки нормального размера; PiP закреплён. Нужен дабл-чек. |
| C3 | Лобби «Комната ожидания» (весь flow) | ⬜ | **Найдено: flow лобби НЕ реализован вообще.** `CallViewModel.handleSocketMessage` обрабатывает только 2006/2007/8001; события лобби 2002 (запрос) / 2003 (LET_JOIN) / 2004 (CALL_USERS) / 2005 (CALL_PARTICIPATE) / 2008 (статус) — не обрабатываются. Нужно: (1) участник в лобби (status 6) видит «ожидание», по 2004/2005 — джойнится; (2) админ видит лобби-участников и жмёт «Разрешить» (REST status→5 или permits); (3) фикс UI: своё видео не перекрывает кнопку «Разрешить». Фронтовая задача, НЕ бэкенд. Это же покрывает B2 «вход после разрешения из лобби». |
| C4 | Реальная пульсация громкости аудио на аватарках (VU-метр) | ⬜ | Зависит от Flashphoner SDK (audio level). Проверить наличие API уровня звука у Stream. |
| C5 | Реконнект-баги: перепроверить (вылет/возврат, медиа, «не слышат») | 🔧 | Часть фиксов уже в ветке (плашка, video-restore, publish-retry). Перепроверить на 2 эмуляторах + Pixel. |
| C6 | Клавиатура/открытые окна/неоднозначное поведение вход/исход | ⬜ | Расплывчато — конкретизировать на прогоне. |

## Приоритет 1 — Чаты

| # | Задача | Статус | Заметки |
|---|---|---|---|
| M1 | Медиа/файлы: разделение фото-видео vs файлы, выбор из галереи/файлов | ⬜ | Большая. fileType #41; отправка `files` в сокет. |
| M2 | Индикация загрузки файлов НА/С сервера (прогресс) | ⬜ | Upload/download progress UI. |
| M3 | Статус сообщения: грузится / отправлено / прочитано (3 состояния) | ⬜ | read #71 (1 отпр., 2 достав., 3 прочит.) + локальный «грузится». |
| M4 | Превью медиа/файлов в списке чатов («Фотография»/«Видео»/«N файлов») | ⬜ | По последнему сообщению диалога. |
| M5 | Фикс блокировки групповых чатов | ⬜ | `block/{dialog}` — проверить работу для групп. |
| M6 | Опросы: статистика активный/завершённый | ➖? | Частично есть; сверить с `poll/{id}/stats`. |
| M7 | Кэш списка чатов | ⬜ | Offline-first для `GET /dialogs`. |
| M8 | Пересылка: отображение «переслано от …» | ➖? | `forwardedFromName` уже есть в модели — проверить UI. |

## Приоритет 2 — Профиль / Лента / Авторизация

| # | Задача | Статус | Заметки |
|---|---|---|---|
| P1 | Профиль: параллакс верхнего баннера при скролле | ⬜ | |
| P2 | Профиль: нижние окна (аватар/функции/изменение) | ⬜ | |
| P3 | Профиль: кэш своего профиля | ⬜ | |
| F1 | Лента: кэш | ⬜ | |
| F2 | Лента: дёрганые теги в постах (верстка) | ⬜ | |
| A1 | Авторизация: анимация успешного входа (не оставлять поля) | ⬜ | |

## Приоритет 3 — Проект / Кэш / Инфраструктура

| # | Задача | Статус | Заметки |
|---|---|---|---|
| PR1 | Переключатель stage/prod на экране логина | ⬜ | У нас build variants; сделать runtime-переключение окружения. |
| PR2 | Оффлайн-старт с кэшем + баннер «Нет сети» | ⬜ | |
| PR3 | Состояния контента: skeleton/empty/error+retry/maintenance | ⬜ | Унифицировать по экранам. |
| PR4 | Чистка мусора (неиспользуемые файлы/либы) | ⬜ | |
| K1 | Кэш: вкладки (Профиль/Чат/Сфера), медиа/голос в чате | ⬜ | |
| K2 | Реальный размер кэша в Системных настройках | ⬜ | |
| K3 | Очистка кэша кнопкой + при логауте | ⬜ | |

## ⏸ BLOCKED на бэкенде (нельзя без сервера)

| # | Задача | Заметки |
|---|---|---|
| B1 | «Печатает…» (typing) | ПЕРЕСМОТРЕНО: раз у iOS работает — socket-событие ЕСТЬ (в прошлом тесте никто его не эмитил, потому и не видели). UI (аватар+3 точки, футер ленты) + emit/receive СДЕЛАНЫ с предполагаемым именем события `typing` `{dialog, owner}` (ChatDialogViewModel + TypingIndicator.kt). Нужно подтвердить реальное имя/формат события от iOS (1 строка — `TYPING_EVENT`). Тестим эмпирически между 2 нашими клиентами по логу SocketTraffic. Личные+групповые. |
| — | Экран «Запись звонка» | ➖ УЖЕ ЕСТЬ: `CallRecordingSetupScreen.kt` 1-в-1 с дизайном (title/описание/название файла/предупреждение+ссылка/кнопка). |
| B2 | Перестали приходить сокеты: удаление сообщений, вход после лобби | По логу приходит только `message`. Проверить: не приходят с сервера ИЛИ приходят, но не обрабатываем (`action.type=delete`, лобби-2004/2005). Если сервер не шлёт — это бэкенд. |
| B3 | Превью видео/миниатюр не сразу (нужен перезаход) | Частично фронт (обновлять по асинхронному превью), частично тайминг сервера. |

## Batch 2026-08-11 (эмуляторный прогон)

| # | Задача | Статус |
|---|---|---|
| B1 | Кнопка ⟳ убрана из панели, смена камеры только в настройках | ✅ |
| B2 | Рука: handUp уже шлётся (проверено), добавлен приём в cType 2006 + wave-бейдж на плитке + тост «админ опустил руку» | ✅ |
| B3 | Выход/завершение звонка → открывает чат диалога (не список) | ✅ |
| B4 | Фон окна был серым (windowBackground=dark_main_background) — поправлен ранее на белый; call-sheet приведён к main_background | ✅ |
| B5 | Локальный «Вы» подтягивает реальный аватар (currentUserAvatarUrl из профиля) | ✅ |
| B6 | Превью видео через серверный ?preview | ✅ |

## Порядок работ

Идём сверху вниз по приоритетам. Каждый пункт: реализация → сборка → установка на устройства → дабл-чек (для звонков — на 2 эмуляторах + Pixel, обязательно сценарий переподключения). Отмечаем статус здесь.
