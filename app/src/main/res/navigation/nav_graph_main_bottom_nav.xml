<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph_main_bottom_nav"
    app:startDestination="@id/nav_graph_chat">

    <include app:graph="@navigation/nav_graph_profile" />
    <include app:graph="@navigation/nav_graph_sphere" />
    <include app:graph="@navigation/nav_graph_chat" />

    <!-- Чужой профиль: глобальный пункт, доступен из любого таба (чат/лента/звонок). -->
    <fragment
        android:id="@+id/otherProfileFragment"
        android:name="uddug.com.naukoteka.ui.fragments.profile.other.OtherProfileFragment">
        <argument
            android:name="userId"
            app:argType="string" />
    </fragment>

</navigation>
